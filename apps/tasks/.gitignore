/node_modules
/.next/
/playwright-report/
/test-results/
/out/
/build
*.tsbuildinfo
next-env.d.ts
.env*
# The template carries no secrets and is the only durable record of the
# variable surface, so it must survive a fresh clone.
!.env.example
.DS_Store
.vercel
