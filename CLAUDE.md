# Ryan Meetup Monorepo Instructions

Follow the repository-wide instructions in `AGENTS.md` and any more specific
`AGENTS.md` in the directory being changed.

For responsive actions, a standalone button must span the available width on
mobile and may return to its intrinsic width at `sm` or above. Button groups may
use a different mobile layout when the relationship between their actions
remains clear.
